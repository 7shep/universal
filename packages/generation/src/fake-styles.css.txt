@import './styles/tokens.css';
@import './styles/components.css';
@import './styles/pages.css';

* { box-sizing: border-box; }
body { margin: 0; min-width: 320px; background: var(--ink); color: var(--paper); font-family: Arial, sans-serif; }
a { color: inherit; }
a:focus-visible { outline: 3px solid var(--signal); outline-offset: 4px; }
.skip { position: fixed; transform: translateY(-200%); }
.skip:focus { transform: none; }
@media (prefers-reduced-motion: reduce) { *, *::before, *::after { scroll-behavior: auto !important; transition: none !important; } }
